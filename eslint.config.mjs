// Disabling all ESLint rules by ignoring all files.
// This is a simple way to turn off linting for the entire project.
const eslintConfig = [
  {
    ignores: ["**/*"],
  },
];

export default eslintConfig;
